name: Hostaway Pipeline

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6,12,18 * * *'

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    env:
      HOSTAWAY_TOKEN: ${{ secrets.HOSTAWAY_TOKEN }}
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas requests

      - name: Run reservations extraction
        run: python extract_reservations.py

      - name: Run finance fields extraction
        run: python extract_finance_fields.py

      # Tâches de ménage (/v1/tasks). Non bloquant : un échec ne doit empêcher ni le rapport ni la
      # publication des réservations. Le script n'écrit son fichier qu'en cas d'extraction
      # complète — sinon le dernier fichier publié reste inchangé.
      - name: Run cleaning tasks extraction
        continue-on-error: true
        run: python extract_cleaning_tasks.py

      - name: Build final report
        run: python build_final_report.py

      - name: Commit generated files
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add -f *.tsv
          git diff --cached --quiet || git commit -m "Automated data refresh"
          git push
